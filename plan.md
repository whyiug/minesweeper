# 轻扫雷 V2：微软商店版 UX 方向的家庭网页扫雷

版本：2.0 设计稿  
日期：2026-09-20  
状态：产品与工程规格；尚未实现、测试或部署。  
适用方式：可独立交给 Coding Agent 实现，无须同时读取上一版。

> 本文替代上一版的产品、视觉和实施优先级。参考对象是 Microsoft Store 的 Microsoft Minesweeper，而不是 Windows 95/XP 灰色复古扫雷。经典规则不等于复古界面。不得只把旧版灰色方格换成蓝色就视为完成。
>
> 用户尚未提供具体版本截图，因此不能宣称已锁定他玩过的主题或构建号。当前采用“现代主题棋盘、丰富但克制的动作反馈、可选自动连开”的方向。下面的配色、布局、动效时长和默认值均为本项目设计，不是对微软程序的实测参数。

## 0. 产品目标与范围

做一个有完整休闲游戏质感的中文网页扫雷，优先适配 Mac 鼠标和触控板，其次是 iPad、手机。网页打开即出现可玩的初级棋盘；不要求玩家注册、登录或安装。程序资源加载后，所有游戏操作在本机执行。部署到 Vercel Hobby，目标为家庭非商业使用、免费额度内运行。

首要目标是“好看、顺手、玩起来有反馈”，而不是做一份扫雷算法演示、复古模拟器或者儿童教学平台。

核心组合：**现代蓝色主题 + 大而清楚的棋格 + 流畅展开与插旗反馈 + 单击/双击/自动连开 + 四档难度 + 可关闭的本机榜**。

V1 必须交付：一套完整主题、正确规则、成熟输入、音效开关、动效开关、响应式布局、简短规则说明、本机榜、测试与静态部署说明。

V1 不做：账号、云存档、棋局恢复、广告、付费、连续签到、等级成长、聊天、头像上传、联机对战、全球榜、数据库、LLM API、大地图冒险或无猜题库工程。

V1.1 候选：第二套花园主题、经过验证的无猜练习、PWA 离线打开。它们不允许挤占 V1 的交互与视觉验收。

### 0.1 不保存状态的精确定义

棋局、随机种子、计时、音量设置、主题选择、当前玩家选择、练习进度全部只在当前页面内存中。刷新或重新打开就是新局，设置回到项目默认值。不要读写 sessionStorage 来绕过这条要求。

唯一可持久化的数据是玩家主动保存的排行榜记录。排行榜可完全关闭；关闭后应用不访问排行榜存储。不保存未完成棋局和逐步操作日志。

缓存应用 JS/CSS/图片不等于保存棋局；未来离线功能也必须保持此边界。

### 0.2 产品默认值

打开进入“标准 · 初级 9×9 / 10 雷”，而不是强制先学教学课。支持启蒙 6×6 / 4 雷，可由家长在配置中设为默认。两个小学生共用同一个产品，不按年龄锁定难度。

连开默认“单击”。“自动”是同等重要、可以明确选择的操作选项，而非藏在开发者设置里。孩子不熟悉自动连开时先用单击；不通过第一次错误插旗偷偷教规则。

## 1. 参考核验与借鉴边界

### 1.1 已核实的信息

Microsoft Minesweeper 的微软官方产品页列出新主题、Auto-Chording、经典模式、冒险模式和每日挑战。[S1][S2]

Xbox 官方帮助中的 Game Options 截图包含声音、计时器显示、问号、结束动画，以及 Clicks to chord 的 Auto / 1 / 2 三种选项。[S3][S4]

该游戏设计负责人 Clay Williamson 的项目回顾将 Auto-Chording 描述为一项可选交互，用于减少玩家在已经完成推理后重复执行的开格动作。他同时明确区分了已发布的自动连开与未发布的 Arcade 原型；本项目不把后者当成现有功能。[S5]

微软也提供 Microsoft Minesweeper 网页入口，可作为浏览器交互参考；不能据此断言网页版本和当前商店版完全相同。[S6]

### 1.2 我们借鉴什么

借鉴主题化的棋盘呈现、显著的格子状态、连续操作时的流畅感、可选择的连开方式、清楚的难度与控制选项。

不复制完整商店大厅、账号体系、广告、XP、每日打卡或订阅。自行绘制棋格、图标、表情与背景，自行制作音效；不从微软安装包提取素材，不使用微软品牌使人误认官方出品。

不用“Fluent 风格”或“Windows 11 风格”这类空泛标签替代可验收的具体设计。没有实际测量就不要声称微软动画持续多少毫秒、某种手势完全一致。

## 2. 视觉设计：先做好一套完整主题

### 2.1 V1 主题：蓝晶

整体为暗蓝灰游戏舞台，略有层次的背景，但不绘制喧宾夺主的风景。棋盘是视觉中心，工具面板服务于操作。不得做成典型 SaaS 仪表盘：没有一排 KPI 卡片，没有大宣传区，没有四处浮动的装饰光球。

| 元素 | 本项目设计 |
| --- | --- |
| 未开格 | 蓝色、轻微光泽与柔和厚度，清晰的边缘；不是 Windows 95 的粗黑白斜边 |
| 悬停 | 亮度小幅提升，边缘轻亮；不抬高整个棋盘、不产生布局变化 |
| 按下 | 很轻的压下/缩放，给手指已命中的即时反馈 |
| 已开格 | 浅蓝白、相对平整，和未开格明显区分 |
| 数字 | 大号实文字体、清楚字重；保留颜色辅助但不能只靠颜色识别 |
| 旗格 | 暖金色强调背景，配深色或红色原创旗帜，旗的轮廓清楚 |
| 连开预览 | 对将要执行的邻格加边缘提示；只是操作预览，不表示系统验证了安全 |
| 触雷 | 标明触发格与其他雷，错旗有独立形状；不全屏红闪 |
| 状态表情 | 原创矢量小表情，有正常、按压、胜利、失败状态，不使用系统 emoji |
| 按钮 | 统一材质和文字样式，和游戏主题协调，不混用三套 UI 组件风格 |

推荐初始色值仅为可调整设计 token：舞台 #172536、面板 #20364D、未开格渐变 #64BCFA → #2683C9、已开格 #EBF5FC、已开格正文 #12304A、旗格 #FFD66B。实际上线前对文字与背景做对比度测试；不在未测量时宣称达到特定无障碍等级。

一套主题必须同时覆盖棋格、菜单、图标、表情、胜负状态、音效与动效。仅改背景色不算第二套主题。

### 2.2 布局

宽屏使用“主棋盘 + 紧凑侧栏”：顶部是标题、难度、帮助、声音和设置；棋盘居左或视觉居中；侧栏显示表情、用时、剩余旗数、新的一局以及本机最好成绩。侧栏建议约 200–220 CSS px，不必占满高度。

内容最大宽度约 1200 px。横向空间不足时，侧栏压缩成棋盘上方/下方工具条，不把格子无限缩小。优先让 13 英寸 Mac 上的初级、中级和必要控件能够同时看到。

窄屏使用“顶部状态条 → 棋盘 → 翻开/插旗切换 → 次要操作”。控件位置在局中保持稳定，胜负结果区出现不挤走棋盘。

初级桌面格子建议 48 px 左右；中级约 32–36 px；高级鼠标布局约 26–30 px。实际尺寸由可用宽高和用户选定的格子大小确定，不强求所有难度同一尺寸。触屏可点区域建议不小于 36 px，空间不足就局部滚动。主要工具按钮目标 44×44 px 或更大。

提供“适合窗口 / 大格子”两档即可，避免复杂缩放工具。高级在手机上可以横屏或滚动；不承诺 30 列同时清晰地放入窄屏。

不锁屏幕方向，不禁止全页双指缩放。核心数字、旗帜和图标不能作为一张整体棋盘图片输出。

### 2.3 首屏与次级面板

首屏就是新棋盘。难度选项可以是简洁的彩色分段按钮或小面板；不用先经过一个大游戏大厅。

设置面板包含连开方式、音效、动效、计时显示、格子大小、本机榜开关。设置保持当前页面内有效。帮助在用户主动打开时解释数字、旗帜、连开，默认不盖住棋盘。

已知会重开的动作采用明确文字“新的一局”。主题、声音、字号/格子大小不重开；难度和连开方式在开局前选择，局中更改会清楚提示“应用并开始新局”，避免一局成绩混合不同连开规则。取消面板不改变当前局。

## 3. 动效与声音：它们是交付功能，不是末尾装饰

下面的时间是本项目初始调参值，不是微软实测值。

| 行为 | 建议表现 | 初始时间范围 |
| --- | --- | --- |
| 悬停/按压 | 微亮、轻压，命中立即可见 | 60–100 ms |
| 单格翻开 | 表面褪去/轻微收拢后显示数字，不做夸张 3D 翻牌 | 100–160 ms |
| 空白区域展开 | 按距起点距离产生短的扩散节奏 | 整次不超过约 300 ms |
| 插旗/取消 | 小幅弹入/淡出，不能把格子挤开 | 120–180 ms |
| 连开 | 对目标邻格统一短促展开，配一个批量反馈 | 120–220 ms |
| 获胜 | 表情变化、简短光带或少量粒子、结果文字 | 约 450–700 ms |
| 失败 | 触发格先突出，其余雷随后淡显，允许立即重开 | 约 200–350 ms |

重要原则：**游戏状态先完整提交，动画只是显示结果。** 不能等逐格动画结束才算胜利、允许下一次操作或更新数字。快速再次点击时，目标格应立即完成尚未结束的装饰动画，让已提交状态可见。

大规模展开只播放一个批次声音，不给每个格子启动声音。使用少量同时存在的动画层，不无限增加计时器、DOM 节点或粒子。新局、切难度和卸载时取消上一局的动效队列，防止旧动画污染新棋盘。

音效至少区分翻开、插旗、连开、获胜、失败。默认静音，声音开关明显；打开后以轻、短、音量稳定为目标，不做背景音乐和爆炸惊吓。可使用 Web Audio 合成少量音色或项目自制音频。浏览器的播放/音频上下文解锁必须从明确的用户操作开始，不依赖自动播放。[S11]

设置支持减少动效，并遵循系统 prefers-reduced-motion；关闭运动后仍有清晰颜色、边框和文字状态变化。[S10]

## 4. 游戏规则与难度

### 4.1 四档难度

尺寸统一为列×行，代码使用 rows/cols 命名。

| ID | 名称 | 列×行 | 雷数 |
| --- | --- | --- | --- |
| intro | 启蒙 | 6×6 | 4 |
| beginner | 初级 | 9×9 | 10 |
| intermediate | 中级 | 16×16 | 40 |
| expert | 高级 | 30×16 | 99 |

这些是本项目预设，不宣称当前微软版本逐项相同。第一版无复杂自定义尺寸面板；家长可以修改一个配置文件后重新部署。

### 4.2 标准规则合同

首次有效翻格后才生成地雷，保护点击格及棋盘内的八邻域。首次插旗不生成棋盘、不启动计时，提前插旗不影响地雷落点。后续不移雷、不偷偷避开错误、不做三条命。

首点及邻域安全不等于整局无猜。V1 为随机标准扫雷，不打“每局都有确定解”“无需猜测”标签。

翻到 0，用迭代队列打开连通空白与边缘数字，跳过旗格，避免递归栈问题。普通开格不能直接打开插旗格。隐藏格和旗格互相切换，V1 不必实现问号第三态。

胜利条件严格为所有非雷格已打开，不要求全部插旗。旗数等于雷数并不自动获胜。终局后不再接受改变棋盘的操作，只能查看与重开。

计数器显示 mineCount - flagCount，不显示成“系统确认剩余地雷”。允许负数并温和提示旗子可能过多，不偷偷夹到 0。

### 4.3 计时

首次有效翻格计时，胜负动作结束计时。采用可注入时钟计算起止差值，不依靠 setInterval 每秒加一；后台会限制定时器执行频率。[S12]

家庭本机榜可采用 Date.now() 差值，切换标签页、最小化、休眠均继续累计；检测到负耗时等明显异常则不允许入榜。它不是权威竞赛时钟，改系统时间或前端代码可以影响结果，本项目不建设反作弊。

不强制 999 秒上限。关闭计时显示仅隐藏 UI，不改变计分语义。

## 5. 连开：三个交互档位

微软官方设置列出 Auto / 1 / 2。[S3][S4] 下列算法和边界是本项目自行定义的明确合同，不宣称已逆向复现微软内部实现。

### 5.1 单击、双击与自动

单击：点一下已开的非零数字；若其相邻旗数等于该数字，翻开其所有未开、未插旗邻格。

双击：同一目标数字在设定间隔内完成第二次点击才执行上述动作。未开格单击仍立即翻开，不为等待双击而延迟所有点击。第一次点开格子产生的第二次点击，不得被识别成对“刚出现数字”的双击连开；第一个点击发生前目标必须已经是已开数字。

自动：在一次有效开格或插旗状态改变之后，对受影响的已开非零数字检查相邻旗数。满足条件就执行连开，并继续处理因此新揭示的相关数字，直到不能继续或终局。不做自动插旗，不做概率猜测，不调用求解器寻找远处安全格。

### 5.2 错旗不能被系统偷偷纠正

三种模式都仅根据可见数字与旗数决定是否尝试连开；不先查询旗子是否真的在雷上。错旗导致目标中有雷时按规则失败。

自动连开旁明确说明：“插旗后可能自动打开周围格子，插错旗也可能触雷。”悬停提示只说明“将打开这些格”，不要写“这些格一定安全”。

自动连开是减少手工动作，不等于无猜模式，也不等于系统证明用户旗子正确。后续的提示系统若实现，不能沿用“用户插旗即为确定雷”的假设。

### 5.3 原子性、队列与一致性

一个显式用户动作及其引发的全部自动连开构成一次规则事务。遍历顺序固定，以便测试和复现。

每次批量开格先检查目标雷，失败优先；处理完整个事务后才判胜，不能因为先遍历到最后一个安全格就忽略同一批次里的地雷。

用受影响数字队列和集合去重，不在 React effect 中反复触发连开。没有新揭示格时退出；终局立刻停止。不能让失败/胜利回调或 React 开发模式重复检查造成重复开格、重复记榜。

记录本局固定的 chordMode。手动单击和双击可归入“手动”榜，自动独立为“自动”榜，避免减少点击后的速度直接混入手动成绩。

## 6. 鼠标、Mac 触控板、触屏与键盘

| 输入 | 翻开 | 插旗 | 连开 |
| --- | --- | --- | --- |
| 鼠标 | 左键 | 右键 | 根据所选档位 |
| Mac 触控板 | 普通点击 | 系统辅助点按或 Shift+点击 | 单击/双击/自动，不强求双键同时按 |
| 触屏 | 翻开模式点按 | 插旗模式点按，也支持长按 | 按档位；主推荐单击或自动 |
| 键盘 | 方向键定位，Enter/Space | F | 手动模式对已开数字 Enter/Space 一次，自动随规则触发 |

双击是指针交互选项，不能要求读屏或键盘用户快速双按；它们发送明确的 CHORD 动作。

使用 Pointer Events 统一手势，指针取消、滚动等情况及时清理。[S9] 只在棋盘区域处理右键菜单，不屏蔽页面其它位置。

长按初值 350 ms、移动容差 10 CSS px；二者待真机调参。长按成功后松手不能再次翻开；拖动滚动结束不能点开；出现第二根手指、pointercancel、失去捕获、页面隐藏时取消未提交动作。

不得在 pointerup、click、touchend 三个通道同时提交规则动作。右键在 pointerup 与 contextmenu 之间去重。键盘/辅助技术合成 click 仍须工作。

按下有视觉反馈、松开才提交，移出/滚动取消。兼容 Mac 的 Ctrl+点击系统上下文菜单路径；不要把读取 click.ctrlKey 当作唯一方案。[S14]

触屏有常驻“翻开 / 插旗”切换，当前模式显著可见，不能只靠长按。棋盘保留滚动与页面缩放，不用 touch-action:none 禁掉所有浏览器手势。

连开指针按下时可以预览将涉及的邻格；松开才执行。不能因为滑过数字自动开格。

### 6.1 无障碍与状态披露

采用可导航网格和 roving tabindex；Tab 进入一次，方向键定位，Escape 关闭面板后还原焦点。[S15]

隐藏格可访问名称只含坐标及隐藏/插旗状态，不暴露真实雷和未打开格的邻雷数。已开数字格不能全部 disabled，因为还需要连开。计时器不每秒打断读屏；结果温和播报。

不使用 color-only 状态编码。窄屏放大后仍可操作，不锁用户页面缩放。

## 7. 失败、获胜和重开体验

失败时清楚指出触发的格子、全部雷和错旗。棋盘保持在原处，侧栏或底部显示结果与“新的一局”。不自动弹一个覆盖整个棋盘的巨大弹窗，也不强制等完整失败动画才能重开。

胜利使用短庆祝、清楚用时和新局入口。只有成功完成后显示保存成绩按钮；不把失败称为玩家能力差，不施加连续签到/连胜压力。

V1 只提供新随机一局，不提供同一雷区重试后继续计入排行榜的入口。未来加入“重玩本题”必须标为练习并排除速度榜。

重开不每次弹确认，但按钮远离棋盘高频点击区域并标识明确。更换难度/连开方式在局中使用简短明确提示，不允许误点后无声丢局。

## 8. 排行榜与隐私

只做本机榜，不要求身份。获胜后选择哥哥/弟弟/家长/访客，主动点击保存。四个名字只是成绩标签，不是账号或可信身份。

按难度与“手动/自动”分别 Top 10，用耗时升序。关闭计时显示不影响资格。无教学辅助、无重玩答案的标准成功局才允许保存。

```ts
interface ScoreEntry {
  id: string;
  gameId: string;
  nickname: '哥哥' | '弟弟' | '家长' | '访客';
  difficulty: 'intro' | 'beginner' | 'intermediate' | 'expert';
  controlClass: 'manual' | 'auto';
  elapsedMs: number;
  completedAt: string;
  rulesVersion: 'modern-v1';
}
```

使用单一版本化 key，例如 light-mines:leaderboard:v2。每局最多一条，保存幂等；清空只删除自己的 key，不调用 localStorage.clear()。解析验证枚举、数量和有限非负耗时；损坏数据或写入失败不影响游戏。

localStorage 按站点来源隔离，不会在 Mac 和 iPad、不同浏览器或不同域名之间自动共享。隐私模式及浏览器策略可能清除或拒绝存储，必须处理异常；不承诺永久保留。[S8]

不采集真实姓名、学校、年龄、联系方式，不加第三方统计 SDK，不主动上传成绩。托管平台仍处理页面请求，不将其宣传为网络链路完全无日志。

家庭跨设备榜只有明确新增需求后再设计中心化存储；第一版不预先装数据库依赖、不留占位云端写接口。

## 9. 技术选型与边界

Vite + React + TypeScript + CSS/CSS Modules，DOM 棋盘，原创 SVG，CSS/Web Animations 动效，少量 Web Audio 或自制音频。游戏核心为纯 TypeScript 函数。

这是针对本项目的选型，不声称微软使用这些技术。默认不引入 Canvas/WebGL、Phaser、Unity、Next.js、SSR 或状态管理大框架。最大预设 480 格，先通过组件边界和事件批处理满足体验，再以测量决定优化。

计时显示独立渲染，不每个显示 tick 重建全部棋格。使用一个界面层消费规则事件，不在多个 Cell 组件里各自处理音频解锁和自动连开。

```text
src/
  app/App.tsx
  config/game.ts
  core/
    types.ts
    generate.ts
    neighbors.ts
    reveal.ts
    chord.ts
    auto-chord.ts
    reducer.ts
    visible-view.ts
  input/
    pointer-controller.ts
    keyboard-controller.ts
  presentation/
    effect-controller.ts
    sound-controller.ts
    reveal-animation.ts
  components/
    GameShell.tsx
    Board.tsx
    Cell.tsx
    StatusPanel.tsx
    GameToolbar.tsx
    TouchModeSwitch.tsx
    SettingsPanel.tsx
    ResultPanel.tsx
    LeaderboardPanel.tsx
    HelpPanel.tsx
  storage/leaderboard.ts
  styles/tokens.css
  styles/theme-blue.css
  styles/game.css
public/icons/
tests/unit/
tests/property/
tests/e2e/
README.md
TEST_REPORT.md
ASSET_NOTICES.md
```

### 9.1 状态与事件

```ts
type Phase = 'ready' | 'playing' | 'won' | 'lost';
type ChordMode = 'single' | 'double' | 'auto';
type Cover = 'hidden' | 'flagged' | 'revealed';

interface GameConfig {
  rows: number;
  cols: number;
  mineCount: number;
  chordMode: ChordMode;
}
```

BoardTruth 留在规则层；展示层只接收经过裁剪的可见棋格，不把真实雷放到隐藏 DOM data 属性/可访问名称中。纯前端不能对开发者工具保密，这只是结构与显示边界，不是防作弊承诺。

时间、随机源、gameId 注入，不在 reducer 内调用 Math.random/Date.now 创建不确定副作用。生成使用可复现随机源与正确无放回采样，不使用随机 sort。

自动连开在规则层完成整个事务。渲染事件带 gameId 和唯一事件 id；音效、动画只消费一次。新局事件使旧队列失效。记榜只能由用户点击且验证资格后执行。

首点完整邻域保护的参数上界：mineCount <= rows*cols - min(rows,3)*min(cols,3)。配置无效显式报错，不偷偷减少雷。

### 9.2 开发环境与脚本

初始化时核对 Vite、测试工具与 Vercel 当前支持的 Node LTS，选兼容版本并在 package.json 和部署配置中固定；不猜测未核实的最新版本号。提交 lockfile，使用 npm ci 重现安装。

提供 dev、typecheck、lint、test:unit、test:e2e、build、preview 和 check 脚本。build 输出静态 dist，不引入运行时秘钥。构建不运行长时间随机题库搜索。

测试建议 Vitest 与 Playwright。浏览器/系统依赖缺失需要如实记录，不报成通过。[S16]

## 10. Vercel 免费静态部署

| 设置 | 值 |
| --- | --- |
| Framework | Vite |
| Install | npm ci |
| Build | npm run build |
| Output | dist |
| 环境变量 | V1 不需要 |
| 数据库 / Functions | 不启用 |
| 计划 | Hobby，家庭个人非商业用途 |

Vercel 官方支持 Vite，Vite 产物为静态资源。[S7] Hobby 有个人非商业用途限制和资源额度，免费不是无限流量或永久不变的保证；发布及未来商业化前重新核对。[S13]

应用只有根路径，设置/帮助/榜单是面板，不为不存在的深层路由添加通配 rewrite。所有核心静态资源随部署发布，不依赖第三方图片站或字体 CDN。

交付部署步骤：提交代码与 lockfile → 导入 Vercel → 确认构建设置 → 部署 → 无痕窗口访问生产域名，确认玩家不需登录 → 家中 Mac 和移动设备测试。

玩家无账号不代表开发者部署不需要账号。没有用户授予的仓库/部署权限时只交付可部署产物与步骤，不宣称已经发布，也不擅自修改 Deployment Protection。

使用固定生产地址给家人，不以每次变化的预览地址承载排行榜。必须在家庭宽带和手机网络实测，不因平台部署成功就声称所有地区访问稳定。

## 11. V1.1：可选扩展，不阻塞 V1

### 11.1 花园主题

更换棋格、旗帜、雷/花图标、表情、背景、交互声音，但保持数字含义与规则一致。不是幼儿园风格的教学页面，不加对话角色不断打断游戏。

### 11.2 无猜练习

只有确实需要后再实现。开发期生成并验证小棋盘题库，开局位置随题一起校验。求解器只能读取已开数字和被证明的结论；玩家旗帜不是确定事实。

支持 R1 剩余雷数为 0、R2 剩余雷数等于未知数、可选 R3 子集差分。不能用真实雷区挑安全格后编造解释；不能将“存在唯一完整雷区”当作当前可见线索足以无需猜测。

提示按“看哪里 → 数量关系 → 下一步”展开，练习不入标准榜。未验证题不得标为无猜。此模块独立于自动连开，自动连开不能替代无猜验证。

### 11.3 离线打开

通过 Service Worker 缓存程序资源，首次成功访问并缓存后支持离线打开；首次从未访问过的网站不能保证离线可用。缓存可能被系统清理。[S17]

离线刷新也开新局，不恢复棋盘。更新不在局中强制重载，不加通知或后台数据同步。

## 12. 测试与验收

### 12.1 规则测试

邻居边界、雷数、首点及邻域保护、不同种子和首点、提前旗子不影响布雷、空白展开、旗格不被普通打开、负旗数、全安全格判胜、仅插满旗不胜、错旗连开失败、批处理失败优先、终局冻结。

自动连开需有正确链式展开、错误旗触雷、多数字共享邻域、重复入队去重、无进展退出、胜负边界和大量固定种子的批量用例。

### 12.2 输入测试

每次手势一个动作；右键去重；Shift 插旗；双击仅对事先已开数字生效；点击新开数字的第二下不得意外变成双击连开；长按后不翻开；滚动不翻开；多指与 pointercancel 清理；键盘独立触发；快捷键不劫持普通表单。

### 12.3 视觉与动效测试

交付桌面初级、桌面中级、桌面高级、窄屏、设置、胜利、失败截图。截图必须来自实际实现，不以概念图冒充运行效果。

交付实际运行录屏或人工可重复的步骤，展示按压、插旗、连开、大面积展开、快速连续点击、快速重开、减少动效与声音关闭。静态截图不能证明交互已经做好。

重开后旧粒子/计时器/音频不能继续修改新局。动画中再次操作不中断规则一致性；波纹不能把尚不可见的数字用于误导用户。后台切回无堆积音效爆发。

### 12.4 数据与发布测试

仅主动保存成绩写榜；刷新不恢复局；关闭榜不访问存储；损坏 JSON 和拒绝写入不崩溃；不同连开类别分榜；一局只保存一次；清空不影响同源其他 key。

Playwright 覆盖可用的 Chromium、WebKit、Firefox；实际 Mac 触控板、Safari 和真实触屏体验另外验收。自动化 WebKit 通过不等同于 iPad 真机通过。[S16]

生产无痕访问无账号门槛；没有运行时数据库/Functions/付费服务；静态资源缺失时可诊断。所有未完成真机测试明确标为未验证。

### 12.5 产品完成条件

“规则测试通过”不等于产品完成。必须同时达到：参考方向不再是复古灰格、所有主要状态视觉完整、交互可连续进行、动效可关闭、Mac 无鼠标顺手、触屏不误触、刷新不恢复棋局、免费静态部署成立。

性能以实际设备测量记录。目标是常见 60 Hz 设备上操作轻快、无可感知长阻塞；不要未测试就给出“全平台稳定 60 FPS”的承诺。

## 13. 实施顺序

### 阶段 A：现代交互样机

先做 9×9 的真实网页样机，覆盖未开、数字、旗、悬停、按压、展开、连开、胜负与工具面板。可使用明确标为开发测试的固定棋盘，但按钮与交互必须真实可用。形成截图与短录屏，确认不是复古灰格，也不是只有一张好看的静态图。

同时建立最小规则测试，避免样机用假的数字和错误交互。此阶段优先证明“游戏看起来、点起来就是想要的方向”。

### 阶段 B：完整规则与输入

补齐四档、首点保护、单击/双击/自动连开、正确胜负、计时、Mac 与键盘输入；规则事务和界面动效解耦。通过规则单测和关键手势测试。

### 阶段 C：触屏、榜单、性能和发布

完成窄屏响应式、真实触屏验收、音效解锁与静音、减少动效、本机榜、跨浏览器测试、构建与部署文档。修复后更新实际截图和测试报告。

不在上述三阶段完成前投入大规模无猜生成、冒险地图或第二套复杂主题。

### 最终交付物

可运行源码、lockfile、静态 dis
## 15. 参考资料

以下外部资料于 2026-09-20 查阅。页面可能持续更新；项目设计提案与外部产品事实须区分。

[S1] Microsoft Store — Microsoft Minesweeper：
https://apps.microsoft.com/detail/9wzdncrfhwcn

[S2] Xbox 官方产品页 — Microsoft Minesweeper：
https://www.xbox.com/en-US/games/store/microsoft-minesweeper/9wzdncrfhwcn

[S3] Xbox Support — Frequently asked questions about Microsoft Minesweeper：
https://support.xbox.com/en-GB/game/microsoft-casual-games/microsoft-minesweeper/support/frequently-asked-questions-about-minesweeper

[S4] Xbox 官方帮助截图 — Game Options / Clicks to chord（图中可见 Auto / 1 / 2；仅供核验，不作为项目素材）：
https://cms-assets.xboxservices.com/assets/21/2f/212f45bb-7ac1-4277-88fc-e3f35555627d.png?n=GameOption-ClicksToChord.png

[S5] Clay Williamson — Microsoft Minesweeper 设计项目回顾：
https://www.claywilliamson.design/projects/7285287

[S6] Microsoft Casual Games / MSN — Microsoft Minesweeper 网页入口：
https://zone.msn.com/en/games/msminesweeper

[S7] Vercel — Vite on Vercel：
https://vercel.com/docs/frameworks/frontend/vite

[S8] MDN — Window.localStorage：
https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
[S9] MDN — Pointer events：
https://developer.mozilla.org/en-US/docs/Web/API/Pointer_events

[S10] MDN — prefers-reduced-motion：
https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion

[S11] MDN — Autoplay guide for media and Web Audio APIs：
https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Autoplay

[S12] MDN — Page Visibility API：
https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API

[S13] Vercel — Hobby Plan：
https://vercel.com/docs/plans/hobby

[S14] MDN — MouseEvent.ctrlKey：
https://developer.mozilla.org/en-US/docs/Web/API/MouseEvent/ctrlKey

[S15] WAI-ARIA APG — Grid Pattern：
https://www.w3.org/WAI/ARIA/apg/patterns/grid/

[S16] Playwright — Introduction：
https://playwright.dev/docs/intro

[S17] MDN — Service Worker API：
https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API
